import React, { FunctionComponent} from 'react';

type Props = {
}

const WordCardWrapper: FunctionComponent<Props> = ({children}) => (
    <div>
        {children}

    </div>
);

export default WordCardWrapper;
